minimumReleaseAge: 10080 # 7 days ; 7 × 24 × 60 = 10080.

shellEmulator: true

trustPolicy: no-downgrade

allowBuilds:
  '@parcel/watcher': true
  esbuild: true

ignoredBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
